# Состояние на 30.08.2026, утро

## Где мы

| | Значение |
|---|---|
| **Лучшая отправка** | **`submission_v20_best.csv` = 1.6481830314** |
| Предыдущая лучшая | `submission_v13_mix.csv` = 1.6491603852 |
| Порог 45-го места | 1.6488895 — **пройден** |
| Порог 15-го места | 1.6469696 — остаётся 0.00121 |

## Сроки и правила (проверено по PDF «Правила E-CUP_23.07.26»)

- **Загрузка решений — до 23:59 30.08.2026** (п. 1.4.3). 5 отправок в сутки, счётчик обнуляется в полночь по Москве (п. 2.9).
- **Выбор двух финальных решений — до 12:00 31.08.2026** (п. 1.4.4, 2.10). Если не выбрать вручную, система возьмёт две лучшие по публичной метрике.
- **Приватный рейтинг существует** (п. 4.2): проверка идёт «на закрытом наборе тестовых данных, недоступном Участникам». Открывается не позднее 12.09 (п. 4.3). Победителей определяет он.
- **Жюри обрабатывает репозитории** при отборе пяти финалистов по каждой задаче (п. 4.5.1).
- Организатор вправе поменять метрику в одностороннем порядке.

Эмпирическая оценка доли публичной части — **около 32%** (из расхождения предсказаний, см. ниже). Согласуется со стандартным делением 30/70.

## Метод, который работает

Отправки образуют линейную оболочку. Каждая пара даёт точное `⟨ly, pᵢ − pⱼ⟩`, а неизвестная `⟨ly,ly⟩` сокращается при условии `Σwᵢ = 1`:

```
MSE(w) = w'Gw − Σwᵢ·Gᵢᵢ + Σwᵢ·MSEᵢ
```

Оптимум по оболочке вычисляется в замкнутой форме **без единой новой отправки**. Формула воспроизводит счета всех отправок точно.

**Подтверждено дважды:** v13 предсказана 1.6491575 → факт 1.6491604 (ошибка 2.9·10⁻⁶); v20 предсказана 1.6481569 → факт 1.6481830 (ошибка 2.6·10⁻⁵).

**Дисциплина обязательна.** Норму весов держать ≤ 5 и не допускать отрицательных прогнозов (обрезка выводит из оболочки и ломает точность). Без ограничения перебор выдаёт веса ±6288 и «счёт» 1.637 — это эксплуатация вырожденных направлений, при 32% публики она развалится на приватной части.

Штраф за подгонку под публику: примерно `2kσ²/n` = **0.0001** при шести коэффициентах и n ≈ 79 000. При прибавке 0.0010 чистый плюс сохраняется.

## Что делать дальше

1. **Новое независимое направление.** v20 ранг оболочки не поднял (он её комбинация), пересчёт даёт +0.00003. Готов `probe_nn_pure.csv` — чистая сеть, приведённая к уровню лучшего файла; новая часть 0.0986, доля новизны 0.79. Собственный счёт ожидается плохим (~1.66), это нормально.
2. **После его счёта** — пересчитать оптимум по расширенной оболочке. Добавление P1 в своё время дало +0.00059 при меньшей новизне.
3. **К 12:00 31.08 выбрать два финальных.** Рекомендация — хеджировать: `submission_v20_best.csv` (максимум ожидания) и `submission_v9_opt.csv` (ни одного подогнанного под публику коэффициента, служит полом).

## Модельный колодец сух

Проверено и закрыто (каждое — прямым измерением):

- полоса сети 360 дней вместо 180 (1.69520 против 1.69494);
- усреднение сидов: деревья +0.00002, сеть +0.00060 в одиночку, но **на уровне ансамбля ноль** — ансамбль уже гасит ту же дисперсию;
- логарифмы блоков (`wl_mean`, `wl_nz`) — ни деревьям, ни сети;
- пол и потолок прогноза (+0.00005), изотоническая (−0.00042), наклон в лог-шкале (b = 0.993–1.003);
- плотность якорей, обрезка признаков, `LayerNorm`, признаки «год назад», подецильная поправка, поправка на остаток;
- множитель калибровки: три отправки одного семейства, вершина параболы 0.96649 против использованных 0.9664.

**Аудит кода — чисто:** утечки нет (418 из 418 колонок воспроизведены из усечённых данных), окна ровно 30 дней, порядок пользователей идентичен, тензор сети индексируется верно, калибровка применяется однократно, `submission.csv` воспроизводится из манифеста до 7.7·10⁻¹².

## Ошибки, которые стоит помнить

- **«12% теста»** — я подставил долю неактивных обучающего пула вместо тестового. Верно **2–3%**, вклад в ошибку 1.5%.
- **«Два чистых якоря»** 2025-09-29 и 2025-10-13 **не независимы**: их целевые окна пересекаются на 16 днях из 30. Это породило ложный «перенос» поправки на остаток (+0.0007), который на борде дал минус.
- **«Решающий тест: в признаках пусто»** — вывод сделан по переобученному фиту с отрицательным R². При нормальной регуляризации там +0.002 внутри якоря. Вывод оказался верным, но по неверному доказательству.
- **Привязка счёта к файлу** — 1.6493157 я принял за `submission_v9.csv`, а это был `v9_opt`. Двое суток выводов строились на неверной базе. Сверять привязку явно перед каждым расчётом.
- Отрицательные результаты систематически отчитывались как установленные, без парных интервалов.

## Заряженные дефекты (обезврежены/помечены)

- `out_work9/val_y.npy` относился к якорю 2025-09-29, а `val_log_*.npy` — к 2026-01-14. `blend.py` спарил бы их молча. **Переименован** в `STALE_val_y_was_2025-09-29.npy`.
- В дефолтном пути `blend.py` безымянный `val_log_two.npy` — вариант `incid`, а `test_log_two.npy` — `noincid`. Использовать только `make_submission.py`.
- `lgb_importance.json` содержит 416 колонок из 423 (нет BTYD) — `--top-feats` молча отсекает лишнее.

## Репозиторий

Приведён в соответствие: исправлены веса ансамбля в README (были 0.35/0.20/0.45, фактически two 0.65 / nn 0.30 / cat 0.05 / lgb 0.00), переписан раздел воспроизведения (добавлен шаг `btyd.py`, задан `CASE3_WORK`, запрещён `blend.py`), обновлён раздел калибровки, помечена двусмысленная строка таблицы.

Осталось на решение пользователя: **LICENSE** не добавлял — вопрос о правах на соревновательное решение не технический.
